export interface Stat {
  icon: string;
  value: string;
  label: string;
}

export interface Feature {
  icon: string;
  iconBg: string;
  title: string;
  description: string;
}

export interface TrustBadge {
  icon: string;
  label: string;
}
